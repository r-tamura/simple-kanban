import { connect } from 'react-redux'
import { CardList } from 'components'
import { toggleCard, getVisibleCards } from 'modules/cards'

const VisibleCardList = CardList

export default VisibleCardList
